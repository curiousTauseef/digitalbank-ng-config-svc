eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    should-unregister-on-shutdown: true
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8400/eureka
    helathcheck:
      enabled: true